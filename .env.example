# Comma-separated list of directories to scan
# These paths should match your Docker volume mounts
SCAN_DIRECTORIES=/data/media,/data/downloads,/data/backups

# Node environment (usually set automatically by Docker)
# NODE_ENV=production

# Treemap Performance Tuning (optional - defaults shown)
# Maximum nodes to render before applying hard ceiling
TREEMAP_MAX_NODES=20000

# Maximum tree depth to display
TREEMAP_MAX_DEPTH=5

# Item count thresholds for adaptive filtering tiers
TREEMAP_LIGHT_THRESHOLD=5000      # Below this: show everything
TREEMAP_MODERATE_THRESHOLD=15000  # 5k-15k: light filtering
TREEMAP_AGGRESSIVE_THRESHOLD=50000 # 15k-50k: moderate, >50k: aggressive
